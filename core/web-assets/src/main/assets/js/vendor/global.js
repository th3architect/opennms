require('vendor/jquery-ui-js');
require('vendor/bootstrap-js');

import Util from 'standalone/util';

window.getBaseHref = Util.getBaseHref;
window.setLocation = Util.setLocation;
window.toggle = Util.toggle;
